---
summary: "CLI reference for `paleoclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `paleoclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `paleoclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `paleoclaw clawbot qr` (same behavior as [`paleoclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `paleoclaw clawbot qr` -> `paleoclaw qr`
